.panel
  .panel-content
    %h1 Cities
    %p We've got the following cities covered and are adding more. Please get in touch if you want to help out.
    - @cities.each do |region, cities|
      %h2= region.capitalize 
      %ul
        - if ! cities.blank?
          - cities.each do |city|
            %li= link_to city, "/?city=#{city}"
        - else
          Stay tuned!
