.panel
  .panel-content
    %h1 please select your city
    - @cities.each do |region, cities|
      %h2= region.capitalize 
      %ul.cities
        - if ! cities.blank?
          - cities.each do |city|
            %li= link_to city.capitalize, "/?city=#{city}"
        - else
          Stay tuned!
